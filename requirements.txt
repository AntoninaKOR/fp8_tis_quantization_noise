# Core dependencies (required for all configs)
torch==2.6.0
transformers==4.40.1
datasets==4.4.1
comet-ml==3.57.4
pyyaml==6.0.2
numpy==2.2.5
pytest==9.0.3
tqdm==4.67.3
accelerate==1.12.0

# Optional: FP8/INT8 rollout (required for backend: flashrl configs only)
# Requires CUDA 12.6, NVIDIA GPU with sm_89+ (RTX 4090) or sm_90 (H100) for FP8
# pip install vllm==0.8.4
# pip install flash-llm-rl
